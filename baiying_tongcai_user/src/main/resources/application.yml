server:
  port: 8083
spring:
  application:
    name: baiying_tongcai_user
  redis:
    host: 127.0.0.1
    post: 6397
    password:
    jedis:
      pool:
        max-idle: 10
        max-wait: 100000
        timeout: 0
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8080/eureka/
cloud:
  config:
    discovery:
      enabled: true
      service-id: springcloud-config-server
    profile: dev
feign:
  hystrix:
    enabled: true
